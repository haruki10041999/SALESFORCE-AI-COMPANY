# Plugin Registry

Unified plugin system for agents, skills, personas, and tool packs.

## Exports

- `plugin-manifest.ts`: Zod schema + validation for plugin manifests
- `plugin-registry.ts`: In-memory registry for loaded plugins
- `plugin-loader.ts`: File-based manifest loader with bootstrap
